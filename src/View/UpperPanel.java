package View;

import Logic.TimeUpdateThread;

import javax.swing.*;
import java.awt.*;

public class UpperPanel extends JPanel {
    UpperPanel() {
        setBackground(Color.black);
        setPreferredSize(new Dimension(10, 40));
        setLayout(new FlowLayout());
        setForeground(Color.white);


    }
}
